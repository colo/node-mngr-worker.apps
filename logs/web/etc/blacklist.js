'use strict'

/**
* any simple property on parsed log, ex: ( http_referer, remote_addr ... not ones added on external modules like referer, geoip...)
**/
module.exports = {
  // remote_addr: /^127\.0\.0\.1$/ //example, dont save log on loopback
}
